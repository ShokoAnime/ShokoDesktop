<UserControl x:Class="JMMClient.UserControls.DashboardEpisodeOverviewTooltipContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal" Margin="5">
        <Border BorderThickness="1" BorderBrush="DarkGray" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Image Width="400" Source="{Binding Path=EpisodeImageLoading}"
                   VerticalAlignment="Top" HorizontalAlignment="Left" RenderOptions.BitmapScalingMode="HighQuality">
            </Image>
        </Border>
        <TextBlock Text="{Binding Path=EpisodeOverviewLoading}" HorizontalAlignment="Left" Foreground="Black" 
                   VerticalAlignment="Top" TextWrapping="Wrap" MaxWidth="300" Margin="5,-1,0,0">
        </TextBlock>
    </StackPanel>
</UserControl>
